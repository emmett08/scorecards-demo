import { FC } from "react";

export const ScorecardsPage: FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <h2>Scorecards</h2>
      <p>
        Use the <strong>Scorecards</strong> tab on an entity page to view the computed compliance for a component.
        This top-level page can be extended to list entities, filter by tags, or aggregate scores.
      </p>
    </div>
  );
};
